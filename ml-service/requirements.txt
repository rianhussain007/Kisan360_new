# CPU-only PyTorch (no CUDA — Render has no GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.5
Pillow>=9.0.0
transformers>=4.30.0
requests>=2.28.0
sentence-transformers>=2.2.0
numpy>=1.24.0

# OpenCV headless (no GUI deps — required for OOD checks in model.py)
opencv-python-headless>=4.8.0
